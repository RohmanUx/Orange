{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^dev"],
      "outputs": [".next/**", "dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "dependsOn": ["^dev"],
      "outputs": []
    },
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": [".next/**", "dist/**"]
    }
  }
}
